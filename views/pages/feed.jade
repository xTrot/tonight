extends ../layout

block headScripts
  script(src="/angular/podsfeed.js")
  script(src="/angular/pods.js")

block content
  .container-fluid(ng-controller="podsController as feed")
    .well.well-sm(ng-repeat="pod in feed.pods" ng-show="{{pod.post.visibility}}")
      pod-header
  